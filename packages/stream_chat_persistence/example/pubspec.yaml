name: example
description: A new Flutter project.

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  # stream_chat_persistence:
  #   path: ../
  stream_chat_persistence:
    git:
      url: https://github.com/ryanheise/stream-chat-flutter.git
      ref: debug.4
      path: packages/stream_chat_persistence

dependency_overrides:
  stream_chat:
    git:
      url: https://github.com/ryanheise/stream-chat-flutter.git
      ref: debug.4
      path: packages/stream_chat

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true